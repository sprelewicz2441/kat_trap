// Single source of truth for each character's display name — used by
// GameScreen (win/lose/pause messages, cutscenes) and CharacterSelectScreen
// (button labels) so the two can never drift apart.
export const CHARACTER_NAMES = {
  CAT: 'Mia',
  MOUSE: 'Poop',
  DOG: 'Dummy',
};
